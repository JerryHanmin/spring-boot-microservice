spring:
  application:
    name: apps-demo2
  cloud:
    config:
      uri: http://localhost:8888/config
    failFast: true
server:
  port: 9006
app:
  groupName: default-group
  eureka:
    hostname: localhost
    port: 8761
spring:
  application:
    name: kafka-consumer
  cloud:
    config:
      uri: http://localhost:8888/config
server:
  port: 9003
app:
  groupName: default-group
  eureka:
    hostname: localhost
    port: 8761